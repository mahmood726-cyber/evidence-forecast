# Numerical baselines

One JSON per PICO capturing expected effect, flip-probability, and representativeness values at a fixed snapshot. Regenerated under a guarded env var; committed as regression anchors.

Populated during Task 17 (live integration) — not before.
